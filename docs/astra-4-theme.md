# Astra 4：切换修复与独立主题

日期：2026-09-10。

## 代码状态

中英文切换修复和此前 astra-3 部署修复已提交为 `4513e1ad`，快进合并并推送至 `origin/develop`。键盘和工具栏的 Mode_switch 现在明确解析为 SWITCH_CHARSET；切换经输入队列串行执行，并在单次引擎操作内读取真实选项、取反及刷新状态。该修复不依赖用于键盘绘制的选项缓存。

新主题在 `feat/astra-theme` 分支开发，独立于已合入主线的修复。布局依据用户提供的 `D:\Downloads\UU Remote Downloads\astra布局.pdf` 两页；图标采用 Phosphor 的 regular yin-yang 路径，转为随按键文字颜色着色的 VectorDrawable，许可证随 APK 打包。

## 主题行为

- 按配置 ID 合并共享目录和用户目录的主题列表，用户版本优先；不会因为名字相同而误删不同主题。
- 新增 `astra.trime.yaml`，显示名 Astra。标准 `tongwenfeng.trime.yaml` 恢复为本地上游镜像 `src` 的内容（行间距 12、键盘高度 250/200、候选字号 18）。
- Astra 继承行间距 8、键盘高度 230/180、候选基准 22sp，以及竖屏短候选的原规则。
- 标准主题中的旧尺寸补丁会在备份后移除，相关源 style 值恢复上游；保留其他 custom 字段。普通外部目录导入后也会在启动时规范化本地副本。相同旧内容仅保留一份基于哈希命名的备份。
- Astra 文本键盘：Z 上方提示 `-`，长按在中文模式输出 `·`、英文模式输出 `_`；逗号键长按分别输出 `、` 和反引号；句号键长按输出 `~`。输出是字面符号，不受 Rime 标点方案二次转换。
- 阴阳图标键保留点击打开符号键盘的动作，上方显示当前浅色/深色；长按切换关联的明暗配色并保存手动选择。这会关闭“跟随系统深浅色”，以使手动选择在下次刷新后仍然有效。
- 数字键盘每行顺序：`{} 1 2 3 + 退格`、`_ 4 5 6 - EN`、`() 7 8 9 * 空格`、`返回 . 0 = / 完成`。
- 不强制切换当前主题；安装后在“键盘样式 → 选取主题”选择 Astra。

## 验证与交付

- 259 项单元测试通过，0 failure/error/skipped；lint 成功。
- 新增测试覆盖两个文本布局的长按动作、数字键顺序、主题去重、标准尺寸还原、补丁备份幂等性以及无平台键名映射时的 Mode_switch 解析。
- 标准主题源文件与 `src` 无差异。完成基于 YAML 的布局预览检查；预览不是 Android 实机截图。
- 只构建 `arm64-v8a`，包名继续为 `com.osfans.trime.debug.fresh`，显示名“同文输入法(Astra)”；交付文件名 `trime-astra-4-arm64-v8a-debug.apk`。
- 仍无三星 ARM64 实机验证。请覆盖安装后测试“中”按钮、双向长按空格切换、符号输出与明暗切换；需要排查时使用日志页的专用维护诊断导出入口。

构建日志：`E:\trime-build-env\astra-4-validation.log`。NAS 目录：`\\192.168.1.171\codex\trime-dev\trime-debug`；同名 `.sha256` 文件记录最终 APK 哈希。
